spring:
  application:
    name: LunchOrder
  profiles:
    active: dev,local

server:
  port: 8080
  servlet:
    context-path: /api/v1

app:
  cors:
    allowed-origins: "http://localhost:4200,http://127.0.0.1:4200"